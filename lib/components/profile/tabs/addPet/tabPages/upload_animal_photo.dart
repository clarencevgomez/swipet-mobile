import 'package:flutter/material.dart';

class UploadAnimalPhoto extends StatelessWidget {
  const UploadAnimalPhoto({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Upload Photos'),
    );
  }
}
